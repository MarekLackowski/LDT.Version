--> 1.7.6 <--

- RIS info in normal mode now puts everything RIS holds about the unit on your desktop screen at once: registration, specification, reject reason, repair, quality checks, cleaning and every earlier pass. It opens locally instead when no other copy of yours is running
- The RIS info button shows a spinner while the request is on its way and confirms once the desktop has the unit
- The program starts faster and puts less load on the machine while it runs
- A field RIS holds in an unreadable format now has an arrow next to the note under it, which puts that text into the field and leaves the cursor at its end

-- 1.7.5.2 --

- Fixed a changed reject reason going to an older pass instead of the one shown on the form

-- 1.7.5.1 --

- A unit picked up from another machine is now announced by a card in the middle of the screen for four seconds, instead of a line of text beside the search bar

-- 1.7.5 --

- Fixed a changed reject reason reaching the wrong pass on a unit that has been through the plant more than once
- Saving a unit RIS holds no registration for now says the reject reason could not be stored
- A unit with a reject reason but no registration at Reject now has its newest registration marked as Reject, and the save says so
- Saving over a field RIS holds in an unreadable format now asks first and keeps what RIS already has
- Today's units in RIS mode now refresh while the tab is open, and the search bar names the serial picked up
- Fixed index, order number, model, reject reason and specs coming back empty on a unit opened from search results
- Repair Status and Repair Result that contradict each other now outline in red, and saving asks first

-- 1.7.4 --

- Fixed a required update opening several prompts at once
- A required update now shows the patch notes of the version being installed
- Quick repairs now show as the number actually done and earn 0.75 points each
- Saving in Repair Database now refreshes the search bar straight away
- An empty label printer address in Settings now means this computer's default Windows printer
- The model line on the RMA label breaks at most once and is never cut short with three dots
- Problem and solution on the RMA label are always printed in full
- Text with nothing to break on is now wrapped at the edge of the line instead of printing over itself

-- 1.7.3 --

- A NAV code search in RMA Mode now also lists rows written up by hand in the workbook, and says where each was found
- The model line on the RMA label now breaks at its slashes instead of shrinking to fit in one piece
- The model and status rows on the RMA label now take the height the problem/solution text does not need
- Fixed the case barcode running off the RMA label at eight digits or more
- Quick repairs done before a unit reaches RIS now show on the leaderboard, counted as half a unit and excluded from the team target

-- 1.7.2.5 --

- The RMA label now grows its text to fill the space instead of staying small
- RMA Mode now finds units by NAV code, listed newest first. Requires the current relay package
- Added a clear button next to Search in RMA Mode
- An ordinary update no longer locks Repair Database, only a required one does

-- 1.7.2 --

- Fixed the sound test playing its tone after the window was closed, and the microphone not being released
- The database log now opens as proper columns in Excel instead of one cell
- Added a Check for updates button to Settings
- Load now falls back to RIS when a serial is not in the shared database
- The program reopens on the monitor, position and size it was closed on
- Search results in RIS mode now show the corrected unit straight after a save
- A forced update now shows the patch notes of the version being installed
- Search results are now one line per unit with columns, about fourteen on screen instead of three
- The selected unit's INOR, reject reason, actions taken and materials used show in a panel below the results
- Search results can be worked from the keyboard: arrows move, Enter loads, Escape closes
- Clicking the search box with nothing typed now lists every unit
- The blue help marks shrink once their help has been shown
- RIS search now recovers on its own after the connection comes back, and says why nothing is listed
- Print Label now asks for confirmation, in Repair Database and in RMA Mode

-- 1.7.1 --

- Fixed garbled characters in place of dashes on the leaderboard tooltip and in this window
- The team target now only says how many the team is over, once it gets there
- Desktop Mode and RMA Mode can now be switched to directly from one another
- Switching mode now offers to restart the program straight away
- A battery reporting over 100% health now shows that figure in red instead of UNKNOWN
- Fixed the battery check running every second when the health could not be read
- Fixed Repair Database occasionally ignoring a typed serial until the program was restarted
- Load now states why a serial could not be looked up
- Relay connections are now renewed periodically, so restarting the relay no longer needs a restart here
- The updater window now sizes itself to what it shows
- Fixed long error messages being cut off in the updater
- Toned down the rank background colours on the leaderboard
- The previous-day rank mark now uses a separate shape for each of the nine ranks

-- 1.7 --

- Supervisors and above can produce a performance report from Account as an Excel workbook
- Added Challenger, a new top rank at 50 units
- The team target bar now carries its own rank, changing colour up to challenger at 75 over
- The team target bar is taller and says how many are left or over
- RMA Mode now shows the planned RIS break warning, the same as Repair Database
- The search box now says what it searches
- Fixed the buttons in Account overlapping the name and company on a narrow window
- Fixed the pinned posts column scrolling the board instead of itself
- Fixed the unit instructions panel not scrolling
- Fixed RMA units counter

-- 1.6.9 --

- Load now accepts the greyed-out serial suggestion instead of looking up your own machine
- Load now says when RIS has no record for a serial
- Typing a new serial and tabbing on now clears the previous unit first
- A planned break in the RIS connection is now announced by a counting-down bar above the form
- The fields that brought up a unit instruction are now outlined in amber
- A model on an instruction no longer has to be the full name
- The mouse test buttons are now outlined instead of dark grey on dark grey
- RIS Database now follows whoever is signed in, and says when an account has no RIS technician
- Signing in or out now clears whatever the previous person had half-written
- Licence keys are now signed and cannot be forged. Everyone needs the new key they were given
- A licence now proves itself without the internet

-- 1.6.8.2 --

- The team leaderboard now has a full ladder from bronze at 5 units to grandmaster at 40, each with its own colour and glow
- Diamond, mystic and grandmaster stand out further, with a heavier edge and deeper glow
- Each technician now carries a gem showing the rank they finished the previous working day on

-- 1.6.8.1 --

- RAM and Memory on a post now take several values, separated by commas
- A post can now match either RAM or Memory instead of both
- RMA Mode can now duplicate an entry for a second identical unit, clearing only serial and case number

-- 1.6.8 --

- Added RIS info to Repair Database, showing everything RIS holds about the serial. Requires the relay update
- A unit that has been through the plant before is flagged, with its earlier passes listed
- A post can now name several order numbers and several models at once
- A post can also be limited to a repair status, a RAM size or a storage size
- Those settings moved into their own section in the post window, closed unless in use
- Instructions now appear as the repair is filled in, not only when the serial is loaded
- Every repair written to RIS is now logged to LDT_DatabaseLog next to the program
- The database log folder can be pointed somewhere else in Settings
- Removed Print Report from Repair Database
- Removed the label preview from RMA Mode
- The program is about 24 MB smaller, and updating clears the files it no longer needs
- A failed update now offers Try again without reopening the program
- Updating and installing now retry a download that drops out
- Installing no longer reports a failure when only an extra tool could not be downloaded
- An extra tool that fails to unpack no longer leaves the previous copy deleted
- Updating and installing now clean up their working folders and check free space

-- 1.6.7 --

- A post can now be attached to an order number, a model or both, and replaces the leaderboard when a matching unit is opened
- Posts carrying instructions now say so on the board, along with what they are attached to
- Deleting a post now deletes its replies as well
- Repair Database now keeps what has been typed when moving between tabs
- Fixed model names printing on top of themselves on RMA labels
- Reject Reason no longer autofills from RIS with slash separated entries
- Fixed the menu background stopping part way down the side of the window

-- 1.6.6.1 --

- Added Send feedback to the menu
- Updating an entry in RMA Mode now keeps its original date instead of today's

-- 1.6.6 --

- Print Label in Repair Database now prints a reject label directly instead of a PDF
- The reject label carries the reject reason, PNR and ProductionOrderLineID from RIS, with barcoded serial and order number
- Added the label printer address to Settings
- A "/" left at the end of Actions Taken or Materials Used is now removed
- Fixed the PNR never reaching the reject label. Requires the relay update
- RIS technician and RMA operator names can now be set while creating a user
- Everything on an RMA label now shrinks to fit, so nothing overlaps
- The status line on RMA labels is bigger
- Fixed the end of a long model name printing over its own beginning on RMA labels
- Fixed a long problem description running over the serial barcode and case number
- Fixed Repair Database search staying empty in RIS mode when the tab was opened before the connection was up
- Repair Database now reloads on its own once the connection comes back
- Load now says when there is no connection instead of clearing the form

-- 1.6.5 --

- RMA Mode can now print a label for a repaired, broker or scrapped unit straight from the entry
- Added a preview of the RMA label
- RMA Mode can be set to 100 x 50 mm or 80 x 40 mm label stock, with everything scaling to fit
- Added a security check requiring an internet connection after a long time offline
- RMA Mode now picks the worksheet from a list instead of having its name typed in
- The Solution field in RMA Mode is now only shown for repaired units
- Broker specifications missing from RIS are now filled in as FALSE, and a missing optical drive as "no ODD"
- A Reject Reason, Actions Taken or Materials Used that RIS holds in another format is now shown under the field
- The Confirm button now shows that a record is being saved
- Choosing the workbook or backup location in RMA Mode now shows that the file is being read
- The file scan in Settings now checks that the updater is present
- The file scan in Settings now reports a worksheet name no longer in the selected workbook
- Label previews no longer pile up in the temporary folder
- Records can no longer be saved with nothing but a serial number
- Fixed RIS statistics and search not working until restart on a fresh installation
- Fixed Actions Taken and Materials Used not autofilling from RIS on a fresh installation
- Fixed Reject Reason autofilling from RIS with entries that only looked like our format
- Removed the "Matching entries" counter outside the shared Repair Database

-- 1.6.4 --

- Diagnostics now opens noticeably faster, at startup and when switching back to it
- Service Board and Account now switch faster
- Reject Reason, Actions Taken and Materials Used now autofill from RIS when written in our format
- RIS Database now has a search bar to find and reopen a repair logged today
- Added a Settings screen in Account, with an option to reset learned suggestions
- Settings can now scan for missing or corrupted files, with a one-click fix for each
- Fixed old data staying in Index, Order Number and Model after typing a new serial
- The RIS Database info screen now only appears when Enable RIS Database is first clicked
- The team leaderboard now shows more entries before scrolling
- The program no longer logs you out on its own

-- 1.6.3 --

- Supervisors can now pin and unpin Basic posts, and Moderators can do the same for Supervisor and Basic posts
- Fixed the gold highlight on the leaderboard being cut off at the edges
- Fixed User Management wrongly listing "Repair" as a protected group name
- Fixed the Touch Test button sometimes not appearing at startup on touch devices
- Fixed the cursor jumping to the end when editing an earlier word in Reject Reason, Actions Taken or Materials Used
- Fixed Reject Reason autocomplete stopping for the rest of the session after such an edit
- Repair Database now greys out without an internet connection
- Removed Guest mode from the Repair Database access screen
- Fixed Tab not reaching the Enable RIS Database button

-- 1.6.2.2 --

- RMA Database now warns when there is no connection to RIS, instead of looking like the device is not registered

-- 1.6.2.1 --

- Fixed the program closing unexpectedly when a technician reached the daily target

-- 1.6.2 --

- Added RMA Mode, switched on in Account next to Desktop Mode
- Reaching the daily target is now marked with an animated gold highlight
- Desktop Mode and RMA Mode no longer start hardware background services, so the program opens faster
- Fixed Service Board sometimes showing read posts as unread again
- Fixed the Service Board notification dot sometimes not appearing for a new post or comment
- Fixed the app logo being slightly cropped

-- 1.6.1 --

- Desktop Mode's exit confirmation now offers Close Program / Close this window instead of the full menu
- The program now checks for updates periodically, not only at startup
- Added an "ⓘ" icon next to the version number to reopen What's New
- Fixed closing What's New in Desktop Mode opening Diagnostics instead of Repair Database
- Fixed the Autopilot status getting stuck on "No Network" after switching tabs
- Fixed Escape not opening the exit confirmation while typing in a Repair Database field

-- 1.6 --

- New app icon
- Repair Database no longer pre-fills Serial with this computer's own serial in Desktop Mode
- Text in Service Board posts and comments can now be selected and copied
- Hovering a pinned post now shows the full post
- Comments are now collapsed by default, with a Reply button to write a new one
- Repair Status now includes "Waiting for parts", counted like Broker/Scrap
- The leaderboard now gives gold, silver and bronze numbers to the top three
- The leaderboard now shows your LDT nickname instead of your RIS name
- Added a daily team target bar at 15 repairs per technician
- A technician's row turns gold at 15 repaired plus broker/scrap for the day
- Fixed a phantom scrollbar in Repair Database on large screens
- Fixed editing a post resetting its publish date
- Fixed Service Board and the team list sometimes showing empty after opening or posting
- Fixed an occasional "403 Forbidden" when saving a repair to RIS
- Fixed Camera Test freezing the program when switching away with the camera running
- Fixed the R/B/P column labels being misaligned
- Camera Test now shows the picture as soon as it is ready instead of waiting a fixed few seconds

-- 1.5.9.1 --

- Tied technicians on the leaderboard now share a rank instead of an arbitrary order
- The "All" column is now "Points", showing each technician's score directly
- The leaderboard now refreshes every 10 seconds while Repair Database is open

-- 1.5.9 --

- Fixed laptops showing "Offline" right after starting with the internet working, now corrected automatically
- New Desktop Mode, turning the program into a dedicated Repair Database terminal
- Fixed the test buttons flashing on screen at startup in Desktop Mode
- Driver Update Tool now has an "Auto reset if needed" checkbox
- Fixed Trial mode silently stopping behaving like Trial after the first launch
- Trial mode can no longer open Repair Database
- The on-screen keyboard button can no longer be reached with Tab
- The leaderboard now ranks fairly, shows everyone instead of the top 3, and no longer miscounts unknown statuses

-- 1.5.8 --

- Added an on-screen keyboard button in Repair Database
- Removed the Date field from Repair Database; RAM and Memory now show in its place
- Fixed the network indicator staying on "Offline" after starting with Ethernet already connected
- Screen Test now jumps to full brightness and restores the previous setting on close
- Microphone and speakers are now unmuted at startup, and the fix now catches the muted device
- Fixed forced updates not showing the prompt until the second restart
- Typing symbols such as "@" no longer triggers keyboard shortcuts

-- 1.5.7 --

- RIS Database mode polished after tester feedback: clearer messages and only what is relevant to the mode
- RIS Database now shows RAM and storage pulled from RIS, editable and saved back
- Reject Reason, Actions Taken and Materials Used suggestions now work in RIS Database mode
- The stats panel now breaks repairs into Repaired, Broker-Scrap and All, and refreshes after saving
- Repair Database now locks with a clear message if the RIS connection drops

-- 1.5.6 --

- New RIS Database mode, for accounts with a RIS technician assigned, syncing repairs straight to RIS
- Removed the old Local Database (beta) option
- Repair Database shows a one-time notice explaining how access works
- Manual technician selection is gone; repairs sync under the technician assigned to your account
- If the RIS option does not appear, log out and back in once

-- 1.5.5 --

- Change Password now requires the current password first
- Fixed error messages not being visible in the Change Password window
- Fixed Camera Test freezing the app when closed quickly or switched while active

-- 1.5.4 --

- Service Board redesigned as a chat: messages grouped by day, replies collapsed by default, a New Post bar in the feed and readable pinned posts
- Account profile redesigned, with Change Password available at any time
- Create Post and User Management redesigned to match, and the Target Groups list no longer gets cut off
- Fixed the technician dropdown not remembering the last used technician
- Cleaned up the updater's status text

-- 1.5.3 --

- Live Device Monitor overhaul - device detection is now far more accurate, and connect/disconnect events are grouped much more clearly
- Driver Update Tool can now be closed with Esc, plus reliability fixes under the hood
- Changed the Camera Test icon

-- 1.5.2 --

- Updates now show real download progress and only download the files that changed
- Various improvements and fixes to the RIS repair sync feature

-- 1.5.1 --

- BETA: Repair Database can now sync finished repairs straight to RIS (Refurbishment Information System)
- This feture will not be available to some users and might become available in a near future.

-- 1.5.0 --

- Audio Test and Keyboard Test windows can now be dragged to move them
- Install Drivers now accessible via hotkey [8]
- What's New overlay can now be closed with mouse click (not just keyboard)

-- 1.4.9 --

- Screen Test: long touch no longer changes color (only short taps)
- Adjusted font size for Diagnostic Buttons

-- 1.4.8 --

- Screen Test: Tab key now switches between multiple monitors
- Driver update log now filters out separator lines
- Install Drivers now checks internet connection
- Fixed ESC key in exit dialog causing crash when focused on Cancel button
- Engine fixes

-- 1.4.7 --

- Fixed buttons on bottom bar
- Board posts now sorted by date (newest first), not by spreadsheet row order
- Notification badge now correctly reflects unread posts after entering Board
- Badge no longer flashes wrong count on startup
- Board scroll fixed after layout change
- Target group selection now uses clickable styled tags instead of manual text input

-- 1.4.6 --

- Fixed a bug causing constant pings about new posts, now read posts are automatically saved as read
- Touch Test: overlays no longer block touch – squares always accessible
- Login shortcuts (Shift+5/Shift+6) now blocked when offline
- License company name now loads immediately when internet becomes available
- Enter key now activates license and logs in (no need to click buttons)
- Diagnostics buttons moved to dedicated bottom bar

-- 1.4.5 --

- Board and Account views now scale properly on different screen sizes (Viewbox added)
- Script Library now supports .psm1 (PowerShell modules) – auto-import and run
- Added BIOS Info utility – displays all BIOS settings with search and color-coded values

-- 1.4.4 --

- "Update Available" now opens changelog window before updating
- Barcode button next to Serial in QuickSpecs – opens scannable barcode window
- Arrow keys added to Keyboard Test layout (▲ ► ▼ ◄)

-- 1.4.3 --

- Live Device Monitor in Diagnostics tab – detects USB, mouse, keyboard, audio, network with smart grouping

-- 1.4.2 --

- Added post pinning (📌) – Admin/Moderator/Supervisor can pin important posts
- Pinned posts appear in a dedicated right-side panel with compact miniatures
- Pinned post miniatures show author tier color, type badge, and group tags
- Notification system – badge on Service Board button shows unread post count
- Notification color reflects the highest-priority unread post
- New posts marked with "NEW" badge on first visit
- Read tracking per user – posts marked read only when entering Service Board
- Delete post now also clears pinned status column

- Added post editing (✏️ button) – Admin/Moderator/Supervisor/Basic can edit own posts
- Added user editing (✏️ button in User Management) – change tier, groups, password
- Protected groups (Trial, Basic, Supervisor, Moderator, Admin) are automatic and hidden from non-Admin
- Post type dropdown now only shows allowed types based on user tier
- Fixed moderator seeing Trial-group posts
- Fixed company field disappearing when editing users as non-Admin
- Side menu buttons are now smaller and better fitting for smaller displays
- New "edited" timestamp when post is modified

--// 1.4.1 //--

- NEW ACCOUNTS AND MESSAGE BOARD SYSTEM!
- Fixed update forcer running updater multiple times
- Analytics now uses network time instead of local system time
- Device ID now uses USB volume serial number instead of generated file
- Replaced HTTP ping internet check with native Windows NCSI (Network Connectivity Status Indicator)
- No more background HTTP requests for connectivity checks
- Fixed intermittent offline flickering on Board/Account tabs
- Redesigned Account view – removed TabControl, simplified to two action buttons
- Added self-inflicted password change

-- 1.3.2 --

- Fixed trial ended screen: "Launch in Trial Version" button now visible and restarts app in trial mode
- Multiple bugs fixed
- Added more licence verification trackers

--// 1.3.1 //--

- Added license system with key activation, 300-launch trial mode, and online validation
- Fixed position of  Battery Panel in Database View
- Added modal overlay with smooth fade animation when dialog windows are open
- Overlay automatically appears/disappears based on active modal windows
- Added critical update forcer thet forces update if current version is lower than required version
- fixed the bug with what's new screen
- fixed problems with License Window

-- 1.2.4 --

- Added battery panel to Repair Database view
- Redesigned table view button with blue theme
- Autopilot check now waits for internet connection, shows "No Network" (yellow) when offline
- Added Network Status indicator in bottom bar with live internet detection
- Renamed Server Status to Database Status
- Added Network Speed Test utility (powered by Ookla Speedtest CLI)
- Shows live progress and final results (ping, download Mbps, upload Mbps)

-- 1.2.3 --

- Added Live Input panel in Keyboard Test showing scan code and VK code for each keypress
- Live Input panel flashes green on keypress
- Added right Windows key to keyboard layout, removed Menu key
- Fixed Autopilot detected state colors in QuickSpecs (now red like other detected values)
- Added "Enable Local Database (beta)" button in Repair Database access window
- Local database creates LDT_Database folder with repairs.json in app directory
- When local mode is active, no password prompt on entering Repair Database
- Server Status correctly reflects local file existence
- Local database auto-creates if missing, with backup recovery option on save
- Added blue circular help buttons for Reject Reason, Actions Taken, Materials Used (local mode only)
  Bugfixes: 
- Help windows now highlight separator format (dot for reasons, slash for actions/materials) with visual examples
- Help buttons excluded from Tab navigation

-- 1.2.1 --

- Fixed Camera Test green border now showing correctly on both mouse click and hotkey
- Moved Update notification from QuickSpecs panel to left sidebar menu
- Update button color changes based on version gap: green (1 step), orange (2-3 steps), red (4+ steps)
- QuickSpecs panel now properly centered vertically
- Restart as Admin now auto-launches Driver Install after elevation
- Fixed progress label width in Driver Update window to prevent bar jumping

--// 1.2 //--

- Redesigned Audio Test window with dark theme, rounded corners and custom title bar
- Input/Output level bars now wider with percentage display
- Microphone sensitivity boosted 4x for better visual feedback
- Redesigned Close buttons in Mouse Test and Screen Test windows to match dark theme
- Memory info now shows DDR type (DDR3/DDR4/DDR5) via SMBIOS in QuickSpecs
- Redesigned Driver Update window with dark theme, progress bar and live logs
- New PowerShell script: installs drivers one by one with per-driver progress reporting
- Progress bar divided equally per driver (0-25% searching, 25-100% per-driver steps)
- Driver count shown as (current/total) without driver names
- Removed ReadKey pauses – process ends cleanly without timeout
- Added Restart as Admin button when admin privileges missing
- Added Restart Now button when reboot required after installation

-- 1.1.8 --

- Redesigned Driver Update window
- Removed the Include quickspecs checkbox; specs are saved only when the entered serial matches the device
- Removed green border from installed utility cards – only "Installed" text remains green, border stays default gray

-- 1.1.7 --

- Fixed utility installation detection – download path now matches the search path in Utilities folder
- Added 1-second loading animation before camera initialization in Camera Test
- Made mouse test and touch test green borders persistent across tab switching
- Experimental: Reject Reason autocomplete now cycles through suggestions every 2 keystrokes based on frequency (most common first, then next most common, etc.)

-- 1.1.6 --

- Prevented dot key (.) from being captured by dev menu while typing in Repair Database text fields
- Fixed Reset button in Repair Database to restore QuickSpecs panel to local machine specs instead of keeping loaded record data
- Update Button updated and moved to Diagnostics View

-- 1.1.5 --

- New design for Search Result Cards
- Generating raport now ignores time and only shows date of generation
- New hidden Numpad layout in keyboard test

-- 1.1.4 --

- Bugfixes related to touchscreen test and keyboard test

--  1.1.3 --

- Added new Touch Screen Test
- New test will only be accesible if the device is recognised as touch device
- Adjusted battery percentage according to company standards
- Fixed detecting keyboard with numpad

-- 1.1.2 --

- Complete keyboard test redesign with dynamic layout detection
- Added automatic numpad detection
- Added system languages display
- Keyboard test now adapts layout based on detected keyboard type
- Added separate numpad section positioned to the right of main keyboard
- Added custom close button (X) with hover effect to all custom windows

-- 1.1.1 --

- If battery Health is unknown, this fact will get properly displayed
- Fixed freezes caused by camera closing arguments
- Fixed buttons in Repair DB Authentification Window

--// 1.1 //--

- Complete UI redesign with dark theme across all windows and dialogs
- Replaced all MessageBox dialogs with custom-styled notifications and confirmations (success, error, warning, yes/no)
- Added smooth animations to all custom dialogs
- Applied dark theme to Repair Database view with proper ComboBox selection display
- Removed intrusive blue focus borders from menu and test buttons
- Fixed camera not stopping when switching tabs
- Fixed ComboBox not showing selected value in Repair Database
- Fixed keyboard shortcuts (Shift+1-4) for tab navigation
- Fixed menu button colors being overwritten at runtime

-- 1.0.8 --

- Fixed audio test playback – sound now restarts from beginning when Play is pressed after completion, and Stop button fully resets the audio position
- Replaced battery serial number display with battery health condition
- Battery condition now color-coded: green (≥85%), yellow (45-84%), red (<45%)
- Battery panel background changes color according to condition for better visual feedback
- Fixed battery voltage display positioning – now correctly shown next to remaining time
- Battery health refreshes every 30 seconds to minimize system overhead

-- 1.0.7 --

- Diagnostics view now uses Viewbox with two‑way scaling
- Right panel (QuickSpecs) also switched to Viewbox with DownOnly scaling – prevents content from being cut off on smaller displays
- Reduced minimum window height from 750 to 650 to better fit laptop screens without overlapping taskbar
- Adjusted layout margins and paddings in Diagnostics view for a more compact, balanced appearance
- Camera selection combobox is now always fully visible, no longer clipped at the bottom of the view

-- 1.0.6 --

- Fixed a bug causing unnecessary focus on contents of Utilities tab
- Centered all label text vertically in QuickSpecs panel 
- Fixed camera loading animation – now properly displays 3-second loading animation on first camera start, with smooth transitions on subsequent uses

-- 1.0.5 --

- Fixed the progress bar in Driver Update jumping between phases
- Added several security based features

-- 1.0.4 --

- Added serial number display to QuickSpecs panel
- Minor bugfixes
- Fixed WMI query in InstalledDriversWindow

-- 1.0.3 --

- Improved driver installation script and added visual support
- Fixed a bug in keyboard test preventing users from using keyboard after closing test window with mouse input

-- 1.0.2 --

- Added WinDirStat utility
- Added autocomplete for Actions Taken and Materials Used fields in Repair Database
- Added support for longer patchnotes

-- 1.0.1 --

- Added HWiNFO utility card
- Fixed icon generation for main executable – now uses multi‑resolution .ico file (16, 32, 64px) for proper display in Windows Explorer and taskbar
- Refined card click animation – scaling effect now triggers only on double‑click, not on single click, for both Utilities and Script Library cards

-- 1.0.0 --

- Added the Utilities tab with downloadable tools, showing installed status and downloading on demand
- Utilities card icons are grayed out when tool not installed, fully visible when installed
- Installer now includes optional utilities with checkbox selection
- Installation progress bar now smoothly animates and shows real download size in MB
- Removed unnecessary start menu shortcut creation from installer
- Removed admin restart requirement – installer now handles permission issues gracefully
- Camera test now shows loading animation while starting camera
- Redesigned exit confirmation dialog – cleaner layout with centered text and buttons, improved readability
- Password dialog (Repair Database Auth) now has centered text input and styled buttons with hover/focus effects
- All action buttons (Print Label, Print Report, Confirm, Update) now have consistent visual feedback
